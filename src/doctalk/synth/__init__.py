"""Phase 4 synthesis layer — the compounding, LLM-authored wiki.

``synth_entities`` (extraction) lands first; ``synth_resolve`` (entity matching) and
``synth_integrate`` (page rewrite + git commit) follow. See ``PLAN.md`` → "Synthesis layer" and
``docs/entity-resolution.md``.
"""
