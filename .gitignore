# Test fixtures — large/binary sample data (BT spec PDF, sample images)
tests/fixtures/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Local config / secrets
.env
*.local

# Derived data stores (regenerable from MySQL — never the source of truth)
*.lance/
data/lance/
data/figures/
# local SQLite dev truth store (DOCTALK_DB_URL=sqlite:///dev.db)
*.db

# Synthesis wiki — a per-instance second source of truth with its OWN nested git repo
# (one commit per ingested source). Runtime data, not repo code.
wiki/

# Frontend (Phase 3 React/Vite app) build artifacts
frontend/node_modules/
frontend/dist/
frontend/*.tsbuildinfo

# OS / editor cruft
.DS_Store
*.swp
.idea/
.vscode/
